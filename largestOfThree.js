let a=-10, b=0, c=-20;
if (a >= b && a >= c) {
    console.log(`Largest is ${a}`);
  } else if (b >= a && b >= c) {
    console.log(`Largest is ${b}`);
  } else {
    console.log(`Largest is ${c}`);
  }